// Published once, from a workflow, to find out whether a registry records the
// workflow it trusts when it first sees a package. Nothing imports this.
export const why = 'a question that could not be answered by reading the documentation';
