{
  "name": "pagebeam-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.6"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "tsc --build && tsc -p tsconfig.test.json && node --experimental-strip-types --test packages/*/test/*.test.ts",
    "typecheck": "tsc --build && tsc -p tsconfig.test.json"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
